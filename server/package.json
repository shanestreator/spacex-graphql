{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node bin/www",
    "client": "cd ../client && npm start",
    "server": "nodemon bin/www",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "prod": "cd ../client && npm install && npm run build && cd ../server && npm install && cross-env NODE_ENV=production node bin/www"
  },
  "author": "",
  "license": "ISC"
}
